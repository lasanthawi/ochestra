# =============================================================================
# WHERE TO GET EACH KEY (copy to .env and fill in)
# =============================================================================
# ANTHROPIC_API_KEY     → https://console.anthropic.com/ (API Keys)
# ENCRYPTION_KEY        → Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NEXT_PUBLIC_STACK_*   → https://app.stack-auth.com (create project → Settings → Keys)
# DATABASE_URL          → https://neon.tech (create project → Connection string, PostgreSQL)
# NEON_API_KEY          → https://neon.tech (Account → API Keys)
# ASSISTANT_API_KEY     → From your Assistant/backend API if you use it
# FREESTYLE_API_KEY     → https://freestyle.sh (if using Freestyle sandboxes)
# SENTRY_DSN            → https://sentry.io (project → Client Keys, optional)
# =============================================================================

# By default Orchestral brain uses Anthropic's Haiku model. Change this in src/mastra/agents/
ANTHROPIC_API_KEY=

# Encryption key for sensitive data (API keys, secrets)
ENCRYPTION_KEY=

# Neon Auth environment variables for Next.js
NEXT_PUBLIC_STACK_PROJECT_ID=
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=
STACK_SECRET_SERVER_KEY=

# Neon Datrabase connection string
DATABASE_URL=

# Neon multitenant API key to provision new projects (must be from a standalone Neon account:
# https://console.neon.tech → Account → API Keys. Do not use a key from Vercel integration.)
NEON_API_KEY=

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Assistant UI (for config validation; use your app URL e.g. http://localhost:3000)
NEXT_PUBLIC_ASSISTANT_BASE_URL=
# Assistant Cloud API - REQUIRED: your project-specific URL from https://cloud.assistant-ui.com
# Format: https://proj-XXXXX.assistant-api.com (NOT backend.assistant-api.com - that returns 403)
NEXT_PUBLIC_ASSISTANT_CLOUD_API_URL=
ASSISTANT_API_KEY=

# Freestyle.sh
FREESTYLE_API_KEY=

# Mastra Cloud codegen API route URL - run `bun run mastra:dev` for local dev
NEXT_PUBLIC_MASTRA_API_URL=http://localhost:4111/codegen

# Sentry DSN for error tracking (optional - leave empty to disable Sentry)
# Only set this in production to prevent forked repos from logging to your account
SENTRY_DSN=
